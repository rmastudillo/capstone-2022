from Osvaldo_le_gano_en_unm_pique import lst_fact as ola
import sys
import numpy as np
sys.path.append("/Osvaldo_le_gano_en_unm_pique")

r = 22
c = 1
def genera(vec, desc):

    while arr.sum() != total_of_1:
        nuevo_vec = np.random.randint(2, size=(r, c))
    while nuevo_vec in desc:
        nuevo_vec = np.random.randint(2, size=(r, c))
    return nuevo_vec


vec = np.random.randint(2, size=(r, c))
desc = ola.lst_fact
genera(vec, desc)
